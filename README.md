To install the required AI model, run

```sh
ollama run qwen2.5-coder:7b
```

once in your terminal to install the model.

On Arch Linux, to install `ollama`, run

```sh
sudo pacman -S ollama
```

and

```sh
sudo pacman -S ollama-cuda
```

to add optional support for CUDA.
